<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wanghongra.jufan">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.FLASHLIGHT" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="net.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name="com.zbar.lib.CaptureActivity" />
        <activity android:name=".MyCenter" />
        <activity android:name=".DengjiActivity" />
        <activity android:name=".GongchangActivity" />
        <activity android:name=".TuijianActivity" />
        <activity android:name=".DuixianActivity" />
        <activity android:name=".ShezhiActivity" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".Test">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ReachListActivity"></activity>
    </application>

</manifest>